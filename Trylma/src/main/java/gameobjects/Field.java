package gameobjects;
import javax.swing.*;

/**
 * Abstract field on board, subtypes: inner field, base field. 
 * Possibly visited object with peg as visitor (Visitor pattern).
 * @see InnerField, BaseField
 */
public abstract class Field extends JComponent{
	
}
